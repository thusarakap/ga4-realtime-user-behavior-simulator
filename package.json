{
  "scripts": {
    "start": "node simulator.js",
    "setup": "npx playwright install",
    "setup:deps": "npx playwright install --with-deps"
  },
  "dependencies": {
    "playwright": "^1.58.2"
  }
}
